// --------------------------------------------------
// Radius
// --------------------------------------------------
+prefix-classes('r-')
  for $num in 0..26
    .{$num}
      border-radius: percentage($num / 100)
  .full
    border-radius: 9999px
